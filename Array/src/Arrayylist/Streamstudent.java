package Arrayylist;

public interface Streamstudent {

	
	.sorted((x) -> x.getLastName().
			.filter( y -> vowels.cotains(y.getFirstNam().charAt(0)));
			.forEach(y -> System.out.println(y.getFirstName() + x.getLastName));
			
			students.stream().map(y -> y.getFirstName()).forEach (name -> System.out.println(name));

		}

}
